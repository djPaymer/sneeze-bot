# Telegram бот для отслеживания чиханий

Бот для записи и отслеживания количества чиханий за день.

## Функции

- ✅ Запись количества чиханий за день
- 📊 Просмотр статистики за месяц
- ✏️ Редактирование данных за конкретную дату
- 📅 Просмотр данных за сегодня

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` в корне проекта и добавьте токен вашего бота:
```
BOT_TOKEN=ваш_токен_от_BotFather
```

3. Запустите бота:
```bash
python bot.py
```

## Использование

### Команды бота:

- `/start` - показать справку по командам
- `/add <количество>` - добавить количество чиханий за сегодня
- `/today` - посмотреть количество чиханий за сегодня
- `/stats` - статистика за текущий месяц
- `/stats <месяц> <год>` - статистика за конкретный месяц (например: `/stats 12 2024`)
- `/edit <дата> <количество>` - редактировать данные за дату (формат: ДД.ММ.ГГГГ, например: `/edit 15.12.2024 10`)

### Быстрая запись:

Вы также можете просто написать число в чат - оно будет автоматически записано как количество чиханий за сегодня.

## Примеры использования

```
Вы: 5
Бот: ✅ Записано: 5 чиханий за сегодня (15.12.2024)

Вы: /stats
Бот: 📊 Статистика за 12.2024:
     Всего дней с записями: 5
     Общее количество чиханий: 25
     Среднее за день: 5.0
     ...

Вы: /edit 10.12.2024 8
Бот: ✅ Обновлено: 8 чиханий за 10.12.2024
```

## Структура проекта

- `bot.py` - основной файл бота с обработчиками команд
- `database.py` - модуль для работы с базой данных SQLite
- `config.py` - конфигурация (токен бота, название БД)
- `requirements.txt` - зависимости проекта
- `sneezes.db` - база данных SQLite (создается автоматически)

## Получение токена бота (BOT_TOKEN)

### Пошаговая инструкция:

1. **Откройте Telegram** и найдите бота [@BotFather](https://t.me/BotFather)
   - Можно просто перейти по ссылке или найти в поиске Telegram

2. **Начните диалог** с BotFather и отправьте команду:
   ```
   /newbot
   ```

3. **Следуйте инструкциям BotFather:**
   - Он попросит ввести **имя бота** (например: "Мой бот для чиханий")
   - Затем попросит ввести **username бота** (должен заканчиваться на `bot`, например: `my_sneezes_bot`)

4. **Получите токен:**
   - После создания бота BotFather отправит вам сообщение с токеном
   - Токен выглядит примерно так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

5. **Создайте файл `.env`** в корне проекта:
   ```
   BOT_TOKEN=ваш_токен_здесь
   ```
   - Замените `ваш_токен_здесь` на токен, который дал BotFather
   - Пример: `BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

### Важно:
- ⚠️ **Никому не показывайте токен** - это как пароль от вашего бота
- ✅ Файл `.env` уже добавлен в `.gitignore`, так что токен не попадет в Git
- 🔄 Если токен утерян, можно получить новый через BotFather командой `/token`
